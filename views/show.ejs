<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
</head>
<body>
    <h1>Event Details</h1>
    <div>
        <h3><%= event.name %></h3>
        <h3><%= event.date.toDateString() %></h3>
        <h3><%= event.time %></h3>
        <h3><%= event.venue %></h3>
        <h3><%= event.availableTickets %></h3>
    </div>
    <a href="/events/<%= event._id %>/book">Book</a>
    <form action="/events/<%=event._id%>?_method=DELETE" method="post">
        <button>Delete</button>
    </form>
   
</body>
</html>